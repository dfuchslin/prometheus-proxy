# prometheus-proxy

A service that reads information from various sources and exposes the data via Prometheus metrics. It can be used to collect metrics from devices that don't have native Prometheus exporters, such as a NAD home theater receiver that can be connected via websockets.
